# =============================================================================
# ECONITH :: environment template
# Copy to `.env` and fill in real values. NEVER commit the real .env.
#   cp .env.example .env
# =============================================================================

# --- Application -------------------------------------------------------------
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO

# --- Binance API (use TESTNET keys for development) --------------------------
# Recommended split keys:
# - BINANCE_DATA_API_KEY/SECRET  : market data + websocket collectors
# - BINANCE_TRADE_API_KEY/SECRET : signed trading/account endpoints
# Legacy BINANCE_API_KEY/SECRET are still supported as fallback.
BINANCE_DATA_API_KEY=your_testnet_data_api_key_here
BINANCE_DATA_API_SECRET=your_testnet_data_api_secret_here
BINANCE_TRADE_API_KEY=your_testnet_trade_api_key_here
BINANCE_TRADE_API_SECRET=your_testnet_trade_api_secret_here
BINANCE_API_KEY=your_testnet_api_key_here
BINANCE_API_SECRET=your_testnet_api_secret_here
BINANCE_TESTNET=true
BINANCE_REST_BASE_URL=https://testnet.binance.vision
BINANCE_WS_BASE_URL=wss://stream.binance.com:9443/ws

# --- Macro ingestion (CORE, zero-cost topology) ------------------------------
# St. Louis Fed FRED API key (free registration at fred.stlouisfed.org).
# This is the ONLY keyed macro source; World Bank, IMF, Eurostat and yfinance
# are keyless/open-access and require no configuration.
FRED_API_KEY=your_fred_api_key_here

# --- Storage -----------------------------------------------------------------
DATABASE_URL=postgresql://econith:econith@postgres:5432/econith
REDIS_URL=redis://redis:6379/0

# --- Frontend (browser-side; resolved from the HOST, hence localhost) --------
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_WS_URL=ws://localhost:8000/api/v1/stream/metrics
