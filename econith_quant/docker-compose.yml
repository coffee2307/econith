---
services:
  econith:
    image: econith/econith-quant:stable
    # image: econith/econith-quant:develop
    # Use plotting image
    # image: econith/econith-quant:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: econith
    volumes:
      - "./user_data:/econith/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://econith/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "127.0.0.1:8080:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /econith/user_data/logs/econith.log
      --db-url sqlite:////econith/user_data/tradesv3.sqlite
      --config /econith/user_data/config.json
      --strategy SampleStrategy
